# VectorDock frontend

Next.js 15 App Router · shadcn/ui · TanStack Query · Zustand

```bash
cd frontend
npm install
cp .env.local.example .env.local   # optional
npm run dev
```

- `NEXT_PUBLIC_API_URL`: FastAPI backend address (default: `http://localhost:8000`).
